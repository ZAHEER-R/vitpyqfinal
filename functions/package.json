{
  "name": "vitpyq-functions",
  "description": "VIT PYQ's Cloud Functions — OTP email & hooks",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "nodemailer": "^6.9.15"
  }
}
